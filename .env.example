# DeepSeek API Key（必填）
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# JWT 签名密钥（必填，建议 32 位以上随机字符串）
JWT_SECRET=your-random-jwt-secret-here

# 服务端口（可选，默认 3000）
PORT=3000

# SQLite 数据库路径（可选，默认项目目录下 data/app.db）
DB_PATH=./data/app.db
